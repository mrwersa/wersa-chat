import { NgModule } from "@angular/core";
import { CommonModule } from "@angular/common";
import { FlashProvider } from "./flash/flash.provider";

@NgModule({
  declarations: [],
  imports: [CommonModule],
  providers: [FlashProvider],
})
export class ProvidersModule {}
